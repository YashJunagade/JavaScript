"use strict"; //treat all JS code as newer version
/* Data Types in JS

- Primitive data types: number, string, boolean, symbol, null and undefined.
    Number 
    String => "", '' (we can use both single quote and double quote)
    Boolean => true/false
    null => standalone value
    undefined => value is not declared
    symbol => used for unique identifiers
    BigInt => used for large numbers

- Reference data types: array and object
    object => {key:value}
*/


let name = "Yash" 
let age = 19 
let adult = true 